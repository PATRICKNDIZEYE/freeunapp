-- This migration was already applied but failed due to enum issues
-- Creating a dummy migration to resolve the failed state
-- The actual schema changes are now handled by the newer migration

SELECT 1;
